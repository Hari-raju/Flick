<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Flick" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:textColor">@color/black</item>
        <item name="hintTextColor">@color/black</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="textColors" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <!-- Background color -->
        <item name="backgroundTint">#efefef</item>
        <item name="textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="viewBg">
        <item name="android:background">#25000000</item>
    </style>

    <style name="vectorColor">
        <item name="android:tint">@color/black</item>
    </style>

    <style name="imageColor">
        <item name="tint">@color/black</item>
    </style>

    <style name="Theme.Flick" parent="Base.Theme.Flick" />
</resources>